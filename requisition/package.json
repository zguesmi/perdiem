{
  "name": "@perdiem/requisition",
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc --project tsconfig.build.json",
    "start": "node dist/index.js",
    "test": "tsx --test 'test/**/*.test.ts'",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@hono/node-server": "^2.1.1",
    "@perdiem/core": "workspace:*",
    "hono": "^4.13.7"
  },
  "devDependencies": {
    "@perdiem/tsconfig": "workspace:*",
    "@types/node": "^22.20.1",
    "tsx": "^4.23.0",
    "typescript": "^6.0.3"
  },
  "version": "0.0.0",
  "description": "Turns one English sentence into a committed, funded auction: parse, hash, approve the spend, lock the budget.",
  "private": true
}