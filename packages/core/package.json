{
  "name": "@perdiem/core",
  "version": "0.0.0",
  "description": "Types, schemas, canonical JSON and hashing shared by every Perdiem package.",
  "main": "./src/index.ts",
  "scripts": {
    "test": "tsx --test 'test/**/*.test.ts'",
    "build": "tsc --project tsconfig.build.json",
    "typecheck": "tsc --noEmit",
    "generate:fixture": "tsx scripts/generate-fixture.ts"
  },
  "files": [
    "dist",
    "src"
  ],
  "private": true,
  "types": "./src/index.ts",
  "type": "module",
  "dependencies": {
    "viem": "^2.56.3",
    "zod": "^4.5.4"
  },
  "devDependencies": {
    "@types/node": "^22.20.1",
    "tsx": "^4.23.13",
    "typescript": "~6.0.3"
  }
}
