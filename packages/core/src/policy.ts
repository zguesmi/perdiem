/**
 * The Policy is the buyer's private ruleset. Only its hash reaches the chain.
 *
 * The shape below is a placeholder. The real schema is settled by
 * .scratch/build/01-policy-schema-and-scoring-formula.md, and nothing should be written against
 * this type until that ticket is resolved.
 */
export type Policy = Record<string, unknown>;
