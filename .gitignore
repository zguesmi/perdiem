node_modules/
.env
.env.*
!.env.*.example
*.local
*.local.*
.DS_Store
*.log
.pnpm-store/
.pnpm-cache/

# Hardhat Ignition's record of what it deployed. The addresses reach the rest of the repository
# through the environment file, and the artifacts beside them are a megabyte per deployment.
onchain/ignition/deployments/

!docs/scratch/verification/evidence/*.log
