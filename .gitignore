node_modules/
.env
.env.*
!.env.*.example
*.local
*.local.*
.DS_Store
*.log
.pnpm-store/
.pnpm-cache/

# Hardhat Ignition's record of what it deployed. A local node is a new chain on every restart, so
# only the Arc deployment is worth keeping.
onchain/ignition/deployments/*
!onchain/ignition/deployments/arcTestnet/

!docs/scratch/verification/evidence/*.log
.claude/worktrees
