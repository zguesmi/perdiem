{
  "name": "@perdiem/purchaser",
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "tsx src/index.ts",
    "test": "tsx --test 'test/**/*.test.ts'",
    "typecheck": "tsc --noEmit",
    "check:intent": "tsx scripts/check-intent.ts",
    "privy:policy": "tsx scripts/privy-policy.ts",
    "check:funding": "tsx scripts/check-funding.ts"
  },
  "dependencies": {
    "@hono/node-server": "^2.1.1",
    "hono": "^4.13.7"
  },
  "devDependencies": {
    "@types/node": "^22.20.1",
    "tsx": "^4.23.0",
    "typescript": "^6.0.3"
  },
  "version": "0.0.0",
  "description": "Turns one English sentence into a committed, funded auction: parse, hash, approve the spend, lock the budget.",
  "private": true
}
