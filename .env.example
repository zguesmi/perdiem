# Shared by every process. Nothing secret belongs here: each package keeps its own secrets in its
# own .env, so that a process only ever holds what it is allowed to hold.

ARC_RPC_URL=
ARC_CHAIN_ID=5042002

# Filled in after deployment.
SEALED_AUCTION_ADDRESS=

# Blocked on .scratch/verification/issues/05-arc-usdc-address-and-decimals.md
USDC_ADDRESS=
USDC_DECIMALS=
