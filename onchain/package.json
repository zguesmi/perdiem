{
  "name": "@perdiem/onchain",
  "version": "0.0.0",
  "type": "module",
  "devDependencies": {
    "@nomicfoundation/hardhat-ignition": "^3.1.8",
    "@nomicfoundation/hardhat-toolbox-viem": "^5.0.7",
    "@types/node": "^22.20.1",
    "forge-std": "github:foundry-rs/forge-std#v1.16.2",
    "hardhat": "^3.16.0",
    "typescript": "~6.0.3",
    "viem": "^2.56.3"
  },
  "description": "The SealedAuction contract: escrow, the auction state machine, and the settlement the enclave reports.",
  "private": true,
  "scripts": {
    "build": "hardhat compile",
    "test": "hardhat test",
    "typecheck": "hardhat compile && tsc --noEmit"
  }
}
