# Wire the five panels to the chain and the relay

Status: ready-for-agent
Blocked by: 04, 07

Intent, funding, bids, enclave, settlement. Every panel shows a transaction hash or a log line.
Bids show commitment hashes only until settlement, then the full bids, so the audience sees why the
cheapest one lost.

The enclave panel tails the simulation log and must never show the preferences.

## Comments
