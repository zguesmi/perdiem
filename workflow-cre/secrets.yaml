# Maps the id `runtime.getSecret({ id })` asks for to the environment variable holding the value.
# It holds no values. In simulation the CLI reads that variable from the file passed with `-e`; on a
# deployment the Vault DON releases the value straight into the attested enclave.
secretsNames:
  ENCLAVE_PRIVATE_KEY:
    - ENCLAVE_PRIVATE_KEY
