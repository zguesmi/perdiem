{
  "name": "@perdiem/workflow-cre",
  "version": "0.0.0",
  "description": "The Chainlink CRE workflow. Scoring happens inside the confidential handler and nowhere else.",
  "private": true,
  "type": "module",
  "scripts": {
    "simulate": "cre workflow simulate . --non-interactive --target staging-settings --trigger-index 0 -e ../.env.arcTestnet",
    "test": "tsx --test 'test/**/*.test.ts'",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.20.1",
    "tsx": "^4.23.13",
    "typescript": "~6.0.3"
  },
  "dependencies": {
    "@chainlink/cre-sdk": "1.20.0",
    "zod": "^4.5.4"
  }
}
